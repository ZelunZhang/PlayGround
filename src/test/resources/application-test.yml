client:
  base:
    url: http://localhost:9091

feign:
    client:
        config:
            default:
                connectTimeout: 5000
                readTimeout: 5000
                loggerLevel: FULL

logging:
  level:
    org:
      springframework:
        cache: TRACE